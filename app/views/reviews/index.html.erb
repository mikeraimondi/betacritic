<%= link_to 'Add a review', new_movie_review_path %>

<h1>Add a review</h1>

<table>
  <tr>
    <th>Body</th>
  </tr>

<% @reviews.each do |review| %>
  <tr>
    <td><%= review.user %></td>
    <td><%= review.body %></td>

  </tr>
<% end %>
</table>
<br />
<% end %>
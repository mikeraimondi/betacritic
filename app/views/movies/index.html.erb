<%- if user_signed_in? %>
  <%= link_to 'Add a new movie', new_movie_path %>
<%- end %>

<%- @movies.each do |movie| %>
  <br />
  <br />
  <%= link_to(movie) do %>
    <%= movie.title %>
    <%= "(#{movie.year})" %>
  <% end %>
<% end %>

<%= render 'shared/recently_added_movies' %>
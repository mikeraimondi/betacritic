<%- if user_signed_in? %>
  <%= link_to 'Add a new movie', new_movie_path %>
<%- end %>

<%- @movies.each do |movie| %>
  <br />
  <br />
  <%- @movie = movie %>
  <%= link_to(movie) do %>
    <%= movie.title %>
    <%= "(#{movie.year})" %>
  <% end %>
  <%= render 'shared/likes' %>
<% end %>

<%= render 'shared/recently_added_movies' %>
<h1>
  <%= @movie.title %>
</h1>

<p>
  <%= @movie.year %>
</p>

<%- if user_signed_in? %>
  <%= simple_form_for([@movie, @like]) do |f| %>
    <%= f.submit 'Like' %>
  <% end %>
<% end %>

<%= "#{@movie.likes.count} Likes" %>

<%= render 'shared/recently_added_movies' %>